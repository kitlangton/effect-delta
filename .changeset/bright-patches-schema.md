---
"effect-delta": minor
---

Add a total schema-derived `delta.schema` with lossless value-schema canonical JSON transport, exact DAG-safe runtime validation, and stack-safe flat sequence encoding.
